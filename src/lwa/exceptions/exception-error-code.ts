export enum LWAExceptionErrorCode {
  access_denied,
  invalid_grant,
  invalid_request,
  invalid_scope,
  server_error,
  unsupported_grant_type,
  temporarily_unavailable,
  unauthorized_client,
  invalid_client,
  other,
}
